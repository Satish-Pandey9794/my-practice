#include <stdio.h>
int main () {
   // incement operator
   // ++i {pre increment operator}
    //i++  {post increment operator}

    /*decrement operator
    --i  {pre decrement operator}
    i--   {post decrement operator}
    */

   int i=1;
//   printf("%d \n",i++);  // use than increase
//   printf("%d \n",i); 

    //printf("%d \n",++i);  // increase than use
   // printf("%d \n",i); 


  //  printf("%d \n",i--);  // use than increase
  //  printf("%d \n",i); 

   // printf("%d \n",--i);  // increase than use
   // printf("%d \n",i); 

   for(char ch = 'a'; ch <= 'z'; ch++) {

    printf("%c \n", ch);   // we can this loop for used in float value
   }

    return 0;
}




/* for (int i=1;  ; i++) {
   printf ("hello world \n");      => this is infinite loop(we can not use this loop because these are damger in my windos)
}*/